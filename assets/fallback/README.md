# Fallback assets

Phase 5 will write precomputed replay assets here, including top-5 prediction JSON and selected feature-map visualisations.

The app should remain usable when this folder is empty.
